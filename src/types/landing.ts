import { CampaignBannerSettings } from "./banner"

export interface LandingSettings extends CampaignBannerSettings {
  heroTitle: string
  heroGlowText: string
  liveBannerText: string
  congonhasStatus: string
}
